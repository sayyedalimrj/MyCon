# Repository Snapshot

This archive contains source/config/docs/tests/docker files plus diagnostics.

Excluded heavy/runtime folders include `data/`, `runs/`, `exports/`, model caches, virtualenvs, and `.git`.

Use `diagnostics/` to inspect git, Docker, WSL, and runtime directory status before server transfer.
